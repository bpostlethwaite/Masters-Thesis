function [ results ] = gridsearchUpper(rec, dt, pslow, v, rrange, hrange)
%GSEARCHKANAMORI Grid Search as implemented in Kanamori, Zhu 2000
%
% Grid search over Tps TPpPs and TPpSs with parameters R (Vp/Vs) and H
% Input Mooneys v


%% Grip parameters
% Vp/Vs Ratio
stack = sum(rec);


end
